<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.magfra.aster">

    <application
        android:allowBackup="true"
        android:icon="@drawable/aster"
        android:label="@string/app_name"
        android:roundIcon="@drawable/aster"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DatiAnagraficiActivity" />
        <activity android:name=".IndirizziActivity" />
        <activity android:name=".UserAreaActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RichiediRimboActivity"></activity>
    </application>

</manifest>